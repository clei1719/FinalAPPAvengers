#include <iostream>
#include <vector>
#include <stdlib.h>

#include "Vertex.hpp"
// Implimentation of just the Vertex(s)
#ifndef Player_HPP
#define Player_HPP

struct Player
{
        Vertex *playerLocation;
};
#endif
